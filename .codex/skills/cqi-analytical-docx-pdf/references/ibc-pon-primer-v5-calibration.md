# IBC PON Primer V5 Calibration Reference

Use this reference only when an IBC report needs to match the existing technical
report family established by:

`/Users/jackyb-cqi/repos/ibc_fiber_network/team-lead-qaqc/tenants/ibc/generated_outputs/ibc_pon_variables_primer/ibc_pon_variables_primer.v5.pdf`

Observed pattern:

- Built from `CQI_DocumentTemplate_20260720.docx`, not from a blank DOCX.
- A4 portrait, 20-page formal report style.
- Preserves CQI cover, document control, index, header, footer, and template
  styles.
- Uses `Light Grid Accent 1` for most tables.
- Uses APA-style `Table N` / `Figure N` labels.
- Every figure has an `Interpretacion (contrato NegritaOS)` block.
- Ends with recommendations, next actions, references/source appendix, and
  traceability appendix.
- Local builder evidence: `build_primer_v3.py` in the same generated output
  folder.

Use this as a visual/fidelity reference, not as domain truth for every IBC EDA.
Do not import obsolete join claims or earlier assumptions from the primer unless
the current analysis evidence still supports them.
