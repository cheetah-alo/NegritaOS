# Pipeline Rules

- Pipelines must declare inputs, outputs, dependencies, and failure behavior.
- Each phase must be independently observable through logs or artifacts.
- Runtime outputs must use repo-defined output folders and avoid ad-hoc root files.
