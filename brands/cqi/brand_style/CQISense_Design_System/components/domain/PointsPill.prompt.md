Scoring contributions, colored by family (repair / risk / neutral / relief).

```jsx
<PointsPill value="+25" family="repair" />
<PointsPill value="0" family="neutral" />
<ChannelSplit tsr="+8" csr="+10" family="repair" />
```

Use `ChannelSplit` for the canonical per-rule TSR/CSR weighting.
