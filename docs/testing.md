# Testing

Use this document as the project-level landing page for test commands and conventions.

Current status: NegritaOS governance validation is covered by local scripts such as `scripts/validate_registry_paths.py`. Add project-specific unit, integration, or E2E commands here when they exist.
