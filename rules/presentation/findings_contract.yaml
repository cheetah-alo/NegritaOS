findings_contract:
  version: "1.0.0"
  purpose: >
    Common intermediate structure used to generate executive presentations and
    Notion documentation from analytical outputs.

  required_fields:
    finding_id: "Stable identifier such as F001."
    message: "One complete English sentence stating the claim."
    evidence: "Metric, chart, table, log, or source excerpt supporting the claim."
    implication: "Why the finding matters for the audience."
    note: "Known limitation, uncertainty, validation need, or what should not be overclaimed."
    recommendation: "Action implied by the finding, or 'None' if not actionable."
    source_path: "Local file path or Notion/source URL used as evidence."

  optional_fields:
    caveat: "Legacy alias for note. Accept for old artifacts, but new outputs must use note."
    owner: "Recommended role responsible for the next action."
    timeline: "Suggested timing when available."
    slide_candidate: "Whether the finding belongs in the main deck or appendix."
    notion_section: "Target Notion section."
    denominator: "Rows, calls, accounts, events, pairs, or model samples used for the metric."
    base_population: "Population/filter used to compute the evidence."
    support_threshold: "Minimum n or support rule used to include the segment."
    outcome_window: "Time window and direction for future/current outcomes, when applicable."
    baseline_reference: "Overall/base rate, control group, or comparison reference used in the claim."

  quality_rules:
    - message_must_be_a_claim_not_a_topic
    - evidence_must_be_traceable
    - plot_based_finding_must_state_base_population_and_denominator
    - rate_or_lift_finding_must_state_baseline_reference
    - future_outcome_finding_must_state_window_and_direction
    - implication_must_be_audience_specific
    - note_must_be_visible_when_uncertainty_exists
    - new_outputs_must_use_note_terminology
    - recommendation_must_not_exceed_evidence

  deck_mapping:
    ceo_product_owner: "message + implication + recommendation in main deck; evidence summarized; note visible."
    data_scientist_peer: "message + evidence + method note in main deck; raw support in appendix."
    mixed_leadership_technical: "executive claim in main deck; technical proof in appendix."
